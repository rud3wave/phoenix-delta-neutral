# 🔥 Phoenix Delta-Neutral Volume Bot

Дельта-нейтральный бот для фарма объёма на [Phoenix Trade](https://www.phoenix.trade) (Solana perps).

Бот открывает противоположные позиции (LONG + SHORT) на разных кошельках, создавая дельта-нейтральный портфель. Это позволяет фармить объём торгов с минимальным рыночным риском.

> Софт работает только с реферальным кодом автора. Код применяется автоматически при регистрации нового аккаунта — ничего вводить не нужно.

---

## Что делает бот

1. **Группировка кошельков** — кошельки объединяются в группы (например, 2 LONG + 1 SHORT), номера групп идут по порядку: 1, 2, 3…
2. **Leader-Follower открытие** — одна сторона открывает лимитки, после заполнения вторая сторона бьёт маркетом точно в те же лоты
3. **Удержание** — позиции держатся заданное время
4. **Умное закрытие** — большинство закрывается лимитками (maker fee 0.005%), меньшинство маркетом (taker fee 0.035%)
5. **TG-уведомления** — два сообщения: при открытии и при закрытии с PnL-отчётом
6. **Забрать награды** — режим 4: собрать реферальные и промо-награды с кошельков
7. **Депозит на биржу** — режим 5: перевести USDC с кошелька на торговый аккаунт Phoenix
8. **Регистрация аккаунта** — новые кошельки онбордятся через Phoenix API (`activate-tx` / `send-register-ixs`), реферальный код применяется сам

---

## Установка и запуск

### 1. Установи Node.js

Скачай и установи Node.js **версии 20+** — https://nodejs.org/en/download

Проверь что версия правильная:
```bash
node -v
# v20.x.x или выше
```

### 2. Установи Git (если ещё не установлен)

Скачай и установи — https://gitforwindows.org/ (всё по умолчанию)

### 3. Создай папку и открой VS Code

Создай папку на рабочем столе (например `phoenix`), открой её в VS Code: **File → Open Folder** → выбери созданную папку.

Открой терминал в VS Code: **Ctrl + `** (обратный апостроф). Убедись что терминал открыт именно в этой папке.

### 4. Клонируй репозиторий

В терминале VS Code выполни:
```bash
git clone https://github.com/rud3wave/phoenix-delta-neutral.git
cd phoenix-delta-neutral
```

### 5. Установи зависимости

```bash
npm install
```

### 6. Настрой global.js

Открой `global.js` и заполни:

- `token` — токен от [@BotFather](https://t.me/BotFather) (для TG-уведомлений, можно оставить пустым)
- `chatId` — твой chat ID от [@getmyid_bot](https://t.me/getmyid_bot) (можно оставить 0)
- `ENCRYPTION_PASSWORD` — пароль для шифрования приватных ключей. **НЕ меняй после первого запуска!**

### 7. Настрой settings.ts

Открой `settings.ts` и заполни под себя:

- `TOKENS_TO_TRADE` — какие токены торговать (например `['ETH']`)
- `MARGIN_MODE` — как считать маржу: `'percent'` (% от баланса) или `'usdc'` (фиксированная сумма в $)
- `MARGIN_RANGE` — диапазон `[мин, макс]`:
  - при `'percent'`: например `[50, 60]` = 50–60% баланса кошелька
  - при `'usdc'`: например `[250, 350]` = $250–350 на позицию
- `LEVERAGE_RANGE` — плечо, диапазон `[мин, макс]`
- `GROUP_CONFIGS` — конфигурация групп `[longCount, shortCount]`. **Должно совпадать с количеством кошельков!** Например: 3 кошелька → `[2, 1]`; 2 кошелька → `[1, 1]`
- `MAX_SPREAD` — максимальный спред для входа (%)
- `HOLD_MINUTES` — время удержания позиций в минутах `[мин, макс]`
- `TRADES_COUNT` — количество циклов `[мин, макс]`

### 8. Добавь приватные ключи

Открой `input_data/privatekeys.txt` и добавь ключи — один на строку.

Поддерживается приватный ключ или мнемоника.

> Для регистрации нового аккаунта на кошельке нужен небольшой запас SOL (рент/комиссии). USDC для торговли кладётся отдельно (режим 5 или вручную).

### 9. Добавь прокси

Открой `input_data/proxies.txt` и добавь прокси — один на строку.

Формат: `http://user:pass@ip:port` или `ip:port`

### 10. Запусти

```bash
npm start
```

Бот предложит выбрать режим:
```
Выбери режим:
  1 = Дельта-нейтральная торговля
  2 = Закрыть все позиции
  3 = Проверить балансы
  4 = Клеймить награды
  5 = Пополнить биржу с кошелька
```

---

## Режимы

**Режим 1 — Дельта-нейтральная торговля**
Основной режим. Бот загрузит кошельки, сформирует группы, откроет дельта-нейтральные позиции, подержит и закроет. Повторит заданное количество раз.

**Режим 2 — Закрыть все позиции**
Экстренное закрытие всех открытых позиций и ордеров на всех кошельках.

**Режим 3 — Проверить балансы**
Показывает баланс USDC на каждом кошельке и общий итог.

**Режим 4 — Клеймить награды**
Нужен, чтобы забрать деньги с кошельков: реферальные награды и награды за промо (например [Flight Club](https://x.com/PhoenixTrade/status/2081831859760083422)).

Объём наторговал / по кампании начислили — на сайте это «Claimable», но само на баланс не падает. Режим 4 проходит по всем кошелькам и забирает доступные награды через sponsorship Phoenix (как на сайте). Без него они просто висят, пока не заклеймишь вручную.

**Режим 5 — Пополнить биржу с кошелька**
Переводит USDC с Solana-кошелька на торговый аккаунт Phoenix (Ember + deposit). Удобно после вывода/клейма наград или перед стартом торговли. Если на кошельке почти нет USDC — кошелёк пропускается.

---

## TG-уведомления

**Сообщение #1 — Позиции открыты**
```
📂 POSITIONS OPENED | Group 1
📊 ETH | Spread: 0.0100% | Mid: $3450.25

🟢 LONG AbCd...WxYz | $180.00 | Lev: 10.2x | Liq: 45.3%
🟢 LONG EfGh...UvWx | $120.00 | Lev: 8.5x | Liq: 52.1%
🔴 SHORT IjKl...StUv | $300.00 | Lev: 9.1x | Liq: 48.7%

🟢 LONG: $300.00 | 🔴 SHORT: $300.00
```

**Сообщение #2 — Позиции закрыты**
```
📂 POSITIONS CLOSED | Group 1

🟢 LONG AbCd...WxYz | 📉 PnL: -0.0312$ | Bal: $195.42 | Vol: $180.00
🟢 LONG EfGh...UvWx | 📉 PnL: -0.0211$ | Bal: $178.31 | Vol: $120.00
🔴 SHORT IjKl...StUv | 📈 PnL: +0.0105$ | Bal: $230.15 | Vol: $300.00

📉 Total PnL: -0.0418$
💰 Total Volume: $600.00
 Cost per 100k: 6.967$
```

---

## Структура проекта

```
phoenix-delta-neutral/
├── main.ts                  # Точка входа
├── settings.ts              # Настройки торговли
├── global.js                # Глобальный конфиг (TG, пароль)
├── input_data/
│   ├── privatekeys.txt      # Приватные ключи (один на строку)
│   └── proxies.txt          # Прокси (один на строку)
├── databases/
│   └── wallets.json         # Зашифрованные ключи (создаётся автоматически)
└── modules/
    ├── utils.ts             # Утилиты
    ├── telegram.ts          # TG-уведомления
    ├── wallet.ts            # Кошельки и шифрование
    ├── phoenix-api.ts       # REST API клиент Phoenix
    ├── phoenix-service.ts   # Торговый сервис
    ├── controller.ts        # Дельта-нейтральный контроллер
    └── referral-guard.ts    # Реферальная защита
```

---

## Troubleshooting

**npm install падает с ошибкой**
Убедись что Node.js 20+ (`node -v`). Очисти кэш и переустанови:
```bash
rm -rf node_modules package-lock.json
npm install
```

**Ошибка аутентификации Phoenix**
Убедись что приватный ключ правильный. Проверь что кошелёк зарегистрирован на [phoenix.trade](https://www.phoenix.trade). Попробуй перелогиниться на сайте и повтори.

**Транзакции не подтверждаются**
Публичный RPC `api.mainnet-beta.solana.com` имеет лимиты. Для стабильной работы используй приватный RPC (Helius, QuickNode, Triton). Замени `SOLANA_RPC` в `modules/phoenix-service.ts`.

**Спред слишком высокий**
Увеличь `MAX_SPREAD` в `settings.ts` (например, 0.05).

**Кошельки не могут покрыть маржу**
Уменьши `MARGIN_RANGE`, поставь `MARGIN_MODE = 'percent'`, либо пополни балансы кошельков (режим 5).

**Регистрация нового аккаунта падает / timeout**
Нужен SOL на кошельке. Регистрация идёт через Phoenix API (не через «голый» публичный RPC). Перезапусти бот — если аккаунт уже создался, повторный онбординг пропустится.

**Депозит падает с ошибкой**
Убедись что на кошельке есть USDC и SOL на комиссию. Режим 5 использует официальный SDK-флоу Phoenix (`buildDepositIxs`).

**Бот зависает на ожидании лимитки**
Увеличь `LIMIT_FILL_TIMEOUT_MINUTES` в `settings.ts`. Проверь что спред не слишком высокий и на кошельке достаточно USDC.

---

## Дисклеймер

Этот софт предоставляется «как есть». Используй на свой риск. Торговля криптовалютами связана с риском потери средств.
